# Exam 2026

## General Infos

- **Time:** 3 h
- **Definitions were given for:**
  - Chebyshev
  - Taylor expansion
  - Hoeffding
  - KL divergence
  - Cauchy-Schwarz
  - Jensen Inequality
- No other sources for help.

---

## Exercise 1 — Linear Algebra

$A \in \mathbb{R}^{n \times n}$

$$A = aa^T + bb^T$$

$$a = u + v, \qquad b = \frac{3}{4}u - 3v$$

**a)** Prove that $A$ is symmetric.

**b)** Calculate all eigenvalues and eigenspaces of $A$.

**c)** What is the rank of $A$?

**d)** What is $\text{tr}(A)$, $\det(A)$ and the singular values?

**e)** Solve the optimization problem:

$$\max_{x \in \mathbb{R}^n,\; \|x\|_2 = 1} \|Ax\|_2 - \langle x, Ax \rangle$$

---

## Exercise 2 — Calculus and Probability

**a)** Given $B$ is an invertible matrix. Simplify this term as far as possible:

$$\frac{\partial}{\partial \alpha} \text{tr}\!\left((B + \alpha X)^{-1}\right) \bigg|_{\alpha = 0}$$

with hint:

$$\frac{\partial}{\partial \alpha} A(\alpha)^{-1} = -A(\alpha)^{-1} \frac{\partial A(\alpha)}{\partial \alpha} A(\alpha)^{-1}$$

**b)** Given a function $f : \mathbb{R}^n \to \mathbb{R}$. Give the Taylor expansion of the function at $x = 0$:

$$f(x) = \sqrt{1 + \|x\|^2}$$

**c)** Given an RV following a multivariate Gaussian distribution $X \sim \mathcal{N}(0, \mathbb{I}\sigma^2)$. Show that the following inequality holds:

$$P\!\left(|Z| > t\sqrt{d\sigma^2}\right) \leq \frac{1}{t^2}$$

**d)** For what dimension $n$ does the following inequality hold?

$$P\!\left(\frac{1}{n}|Z| < 0.01\right) \leq 0.99$$

---

## Exercise 3 — Optimization

$$\max_{x \in \mathbb{R}^n} \sum_{i=1}^{n} x_i\!\left(-\log(x_i) + \log(p_i)\right)$$

$$\text{s.t.} \quad x_i \geq 0, \qquad \sum_{i=1}^{n} x_i = 1$$

**a)** Is this similar to a convex optimization problem?

**b)** Is $\phi(x) = \sum_{i=1}^{n} x_i\!\left(\log(x_i) - \log(p_i)\right)$ strongly convex?

**c)** Given strong duality holds. Write down the Lagrangian, the dual function and solve the dual problem.

---

## Exercise 4 — Mixed Stuff

Give the following answer in "Yes / No" — give a counter example or valid proof for the same.

**a)** If every element in a given matrix $A$ is non-negative, does that imply the matrix $A$ is positive semi-definite?

**b)** Given $Q$ is orthogonal. Prove that $\det(Q) = \pm 1$.

**c)** $B(0,1) = \{x \in \mathbb{R}^n \mid \|x\|_2 \leq 1\}$. Show that for $0 < \varepsilon \ll 1$ the ball $B(0, 1-\varepsilon)$ contains no volume compared to $B(0,1) \setminus B(0, 1-\varepsilon)$ for $d \to \infty$. So the volume is contained in the outer layer of the sphere.

**d)** We know from equivalence of norms: $c\|x\|_1 \leq \|x\|_2 \leq C\|x\|_1$. For $L_1$ norm and $L_2$ norm, find the tightest bounds, hence values for $c$ and $C$.

**e)** KL divergence and ELBO.

$$p(y) = \int p(y \mid w)\, p(w)\, dw$$

Show that:

$$\log p(y) \geq \mathbb{E}_q\!\left[\log p(y \mid w)\right] - D_{\text{KL}}(q \| p)$$

The definition for KL divergence was given. Use hint:

$$\log p(y) = \mathbb{E}_q\!\left[\log\!\left(\frac{p(y \mid w)\, p(w)}{q(w)}\right)\right]$$
